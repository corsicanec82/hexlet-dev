- file:
    path: ~/.bashrc.d
    state: directory

- lineinfile:
    path: ~/.bashrc
    line: |
      # include source files from ~/.bashrc.d directory
      if [ -d ~/.bashrc.d ]; then
        for file in ~/.bashrc.d/*; do
          . "$file"
        done
      fi
